<Window
    x:Class="Shellvis.Shell.Views.StickyWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Shellvis note">

    <!--
        A square of paper, and nothing else on it.

        No title bar, no menu, no toolbar: everything a sticky note offers has to be
        reachable without adding furniture, because furniture is most of a small window.
        So the whole surface drags, the text fills it, and the two controls that exist
        appear only when the pointer is over the note.
    -->
    <Grid x:Name="Surface"
          Background="{ThemeResource StickyYellowBrush}"
          CornerRadius="6"
          Padding="10,8,10,10">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!--
            The strip along the top is the drag handle AND where the controls live. It is
            always present rather than appearing on hover, because a handle you have to
            discover is a handle nobody finds.
        -->
        <Grid x:Name="Header" Grid.Row="0" Height="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock x:Name="Stamp"
                       Grid.Column="0"
                       FontSize="10"
                       Opacity="0.55"
                       VerticalAlignment="Center"
                       Foreground="{ThemeResource StickyInkBrush}" />

            <!--
                Colour first, close second, and the order matters: the destructive control
                sits furthest from where the pointer usually is.
            -->
            <Button x:Name="ColourButton"
                    Grid.Column="1"
                    Width="20" Height="20"
                    Padding="0"
                    Background="Transparent"
                    BorderThickness="0"
                    ToolTipService.ToolTip="Colour">
                <TextBlock Text="&#xE790;"
                           FontFamily="Segoe Fluent Icons"
                           FontSize="12"
                           Foreground="{ThemeResource StickyInkBrush}" />
            </Button>

            <Button x:Name="CloseButton"
                    Grid.Column="2"
                    Width="20" Height="20"
                    Padding="0"
                    Background="Transparent"
                    BorderThickness="0"
                    ToolTipService.ToolTip="Throw away">
                <TextBlock Text="&#xE711;"
                           FontFamily="Segoe Fluent Icons"
                           FontSize="12"
                           Foreground="{ThemeResource StickyInkBrush}" />
            </Button>
        </Grid>

        <!--
            AcceptsReturn, because a note is written in lines. No border and no background:
            the paper is the background, and a box drawn inside a box reads as a form.
        -->
        <TextBox x:Name="Body"
                 Grid.Row="1"
                 AcceptsReturn="True"
                 TextWrapping="Wrap"
                 BorderThickness="0"
                 Background="Transparent"
                 Padding="0"
                 FontSize="13"
                 Foreground="{ThemeResource StickyInkBrush}"
                 ScrollViewer.VerticalScrollBarVisibility="Auto"
                 PlaceholderText="..." />
    </Grid>
</Window>
